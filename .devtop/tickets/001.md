---
assignee: bob
created: '2026-07-26'
id: '001'
priority: urgent
status: in-progress
title: Auth token refresh fails silently
---

When the OAuth token expires, the refresh call fails with a 401 but the error is swallowed by the `fetchWithAuth` wrapper. The user sees no error and the next API call also fails with a 401.

Expected: show a "Session expired, please log in again" toast and redirect to login.

**2026-07-26 14:32** — alice: Reproduced locally. The issue is in lib/auth.py:112. The except block catches the HTTPError but passes silently.

**2026-07-26 09:15** — bob: Fix looks straightforward. I'll handle the toast UI component. Let's pair on this tomorrow.
**2026-07-27 10:49** — New comment here